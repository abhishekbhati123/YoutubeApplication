import { YoutubesafeUrlPipe } from './youtubesafe-url.pipe';

describe('YoutubesafeUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubesafeUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
